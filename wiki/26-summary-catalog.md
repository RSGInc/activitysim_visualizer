# 26 - Summary Catalog

This page is the data dictionary for summary CSV tables from the Output
Processor. It explains what one row represents, how each table is used, and
what each output field means. The category references combine that analytical
context with authoritative file names, schemas, summary functions, and declared
input requirements.

## How to Interpret the Tables

- Weighting follows the analysis unit named in each field. Unless a table says
  otherwise, weighted people sum `per.finalweight`, weighted households sum
  `hh.finalweight`, weighted person-days sum `day.finalweight`, weighted tours
  sum `tours.finalweight`, weighted trips sum `trips.finalweight`, and weighted
  vehicles sum `vehicles.finalweight`.
- A lower-level table can inherit its weight from a person or household, but
  its unit does not change. One person with weight `3.0` and four trip rows
  contributes `3.0` weighted persons and `12.0` weighted trips.
- In an unweighted cache, existing `finalweight` columns are set to `1.0`.
  Builder-specific participant and occupancy adjustments still apply. Measures
  read from assignment tables or externally supplied contracts are not
  automatically reweighted.
- Joint-tour participant expansion uses prepared `NUMBER_HH`, derived from the
  participant table when a tour has participant records, then from
  `number_of_participants`. An existing `NUMBER_HH` is retained as a final
  source before defaulting to one.
- Rates, percentages, averages, standard deviations, medians, percentiles, and
  fields with names such as `geography_count` use the calculation stated in
  their table entry; they do not share one universal weighting rule.
- Values such as `all_geographies`, `all_person_types`, `all_tour_purposes`,
  `all_tour_modes`, `All Modes`, `All Auto`, and `Daily` are rollups. Use either
  the rollup or its component rows, but do not add them together.
- `geography_type` names the configured spatial system, such as MAZ, TAZ,
  county, MPO, or a custom geography. `geography_id` is the identifier in that
  system. Each table description identifies the home, work, school,
  destination, or parking geography.
- Distance values use the units of the prepared distance or skim fields,
  usually miles. Distance histograms use `0` only for exact zero, `>0-<1` for
  positive sub-mile values, and whole-mile lower-bound labels thereafter.
  Missing, nonfinite, and negative distances are excluded. End bins such as
  `40+`, `20+`, or `51+` include all larger values.
- `time_bin` is the prepared ActivitySim period index: 1--24 for hourly inputs
  or 1--48 for half-hour-period inputs. Named `time_period` and `count_period`
  values come from configured or supplied period labels.
- Category codes and labels come from prepared ActivitySim values and the
  configured category mappings. Keep the code field for joins. Use the label
  field for presentation.
- A valid calculation can produce an empty CSV. That is distinct from a summary
  marked unavailable because an input table or field was absent. Use the
  summary manifest to identify the status.

## Build Status

The standard summarize workflow builds **85** tables. Of the **15** tables with
`Default build = no`, two are optional skim ECDF products and 13 are validation
contracts supplied by an external process through `summary_table_map`. The
visualizer does not calculate those contracts from `RunData`. The references
below describe all 100 contracts.

## Summary Table Reference

The tables are grouped by analytical category and identify the related dashboard
pages. Each row combines the user-facing data dictionary with developer details.
Category, analytical-use, logical input-type, and field-description text is
maintained in
[`scripts/summary_catalog_metadata.yaml`](../scripts/summary_catalog_metadata.yaml).
File names, exact output data types, build defaults, summary functions, and
declared input requirements come from the registered summary declarations.

Required-input types give the canonical stored type where prepare guarantees
one and a logical or source-compatible type otherwise; they are not raw
ActivitySim file-type requirements. Output types are the exact stored CSV
contract.

A few categories have additional conventions:

- The Required Tables/Fields column reports each `@summary` declaration's
  mechanical prerequisites. Builder-level aliases and conditional inputs can add
  requirements that the declaration cannot express. For example, joint-tour
  composition needs `composition` or `tour_composition`, composition by party
  size also needs `number_of_participants` or `NUMBER_HH`, and person
  participation by household size needs `HHSIZE` or `hhsize`.
- `jtf_distribution` declares only household inputs. On a one-day run, an
  unavailable tour table can still produce an all-`No Joint Tours` result
  instead of being marked unavailable. Verify tour availability before using
  that distribution.
- Skimjoin diagnostics use `skim_scenario` for the selected mode or a hypothetical
  sidecar mode. `all_records` identifies hypothetical values for all applicable
  records, and each table also includes an all-modes group.
- Processor-built validation summaries that use optional assignment tables are valid
  but empty when those inputs are absent.
- `tour_distance_by_tour_purpose` and
  `average_nonmandatory_tour_distance_by_purpose_and_geography` need
  `tours.SKIMDIST` to produce results, although their current declarations omit
  it from the generated Required Tables/Fields column. This is a code-contract
  gap.
- The 13 externally supplied validation contracts are registered for external CSV
  input. Their builders do not calculate values, so the supplying workflow is
  responsible for consistent units, period definitions, and values.

Regenerate these tables and the standalone processor output reference after
changing summary metadata or declarations:

```bash
uv run python scripts/generate_wiki_catalogs.py
```

<!-- GENERATED:SUMMARY-CATALOG START -->
_Generated from `processor.summarize.catalog.SUMMARY_DEFINITIONS` and `scripts/summary_catalog_metadata.yaml`._

Registered summaries: **100**; built by default: **85**.

### Population and Demographics

_Related dashboard pages: Overview. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `household_size_distribution`<br>`household_size_distribution.csv` | Household totals by modeled household size. Use it to profile household composition, calculate size shares, and compare population-synthesis results across runs. | `hh`: Prepared household table.<br>&bull; `HHSIZE` (`Int32`): Household person count clipped to 1 through 5, where 5 represents households of five or more people.<br>&bull; `finalweight` (`Float64`): Household expansion weight from the configured weight, inverse sample rate, or 1.0 fallback. | `household_size` (`Int64`): number of people in the household.<br>`household_count` (`Float64`): weighted households in that size category. | `processor.summarize.summaries.demographics.hh_size`<br>Default build: **yes** |
| `person_type_distribution`<br>`person_type_distribution.csv` | Person totals by ActivitySim person type, with a display label. Use it to compare demographic market segments and as a denominator for person-type travel rates. | `per`: Prepared person table.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `person_type` (`String`): stable person-type code.<br>`person_type_label` (`String`): configured readable label for the code.<br>`person_count` (`Float64`): weighted people of that type. | `processor.summarize.summaries.demographics.person_type`<br>Default build: **yes** |
| `population_totals`<br>`population_totals.csv` | One run-level control-total row for people, households, person-tours, person-trips, and person-stops. Joint tour and trip records use the sum of all matching participant person weights when available and valid party-size expansion only as a fallback; sentinel, missing, or nonpositive fallback counts resolve to one. Use it for reasonableness checks and top-level comparisons; the measures use their own table weights and are not additive to one another. | `per`: Prepared person table.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br><br>`hh`: Prepared household table.<br>&bull; `finalweight` (`Float64`): Household expansion weight from the configured weight, inverse sample rate, or 1.0 fallback.<br><br>`tours`: Prepared tour table.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights.<br><br>`trips`: Prepared trip table.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback.<br>&bull; `stops` (`Int32`): Intermediate-stop indicator, 1 before the last trip on a half-tour and 0 for the final leg. | `person_count` (`Float64`): weighted persons.<br>`household_count` (`Float64`): weighted households.<br>`tour_count` (`Float64`): weighted person-tours; joint records are participant-expanded.<br>`trip_count` (`Float64`): weighted person-trips; joint records are participant-expanded.<br>`stop_count` (`Float64`): weighted person-trip records flagged as intermediate stops; joint records are participant-expanded. | `processor.summarize.summaries.demographics.population_totals`<br>Default build: **yes** |

### Person Attributes and Long-Term Choices

_Related dashboard pages: Individual Choices, Mandatory Location Choice. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `license_holding_status_distribution`<br>`license_holding_status_distribution.csv` | Licensed and unlicensed people age 16 or older by person type, including an all-person-types rollup. Use it to assess access to driving and explain auto-mode availability. | `per`: Prepared person table.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br>&bull; `has_license` (`Boolean-like`): Whether the person holds a driver's license.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br>&bull; `age` (`integer`): Person age in years. | `person_type` (`String`): person-type code or `all_person_types` rollup.<br>`license_holding_status` (`String`): `has_license` or `no_license`.<br>`person_type_label` (`String`): configured readable person-type label.<br>`person_count` (`Float64`): weighted people in the group. | `processor.summarize.summaries.long_term_person.license_holding_status`<br>Default build: **yes** |
| `bicycle_comfort_level_distribution`<br>`bicycle_comfort_level_distribution.csv` | Bicycle comfort categories by person type, including an all-person-types rollup. Use it to understand the population assumed willing to use different bicycle facilities. | `per`: Prepared person table.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br>&bull; `bike_comfort` (`category`): Modeled bicycle-comfort alternative.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `person_type` (`String`): person-type code or rollup.<br>`bicycle_comfort_level` (`String`): prepared bicycle-comfort category.<br>`person_type_label` (`String`): configured readable person-type label.<br>`person_count` (`Float64`): weighted people in the group. | `processor.summarize.summaries.long_term_person.bicycle_comfort_level`<br>Default build: **yes** |
| `transit_pass_ownership_by_person_type`<br>`transit_pass_ownership_by_person_type.csv` | Transit-pass ownership status by person type, including an all-person-types rollup. Use it to evaluate transit market eligibility and pass-ownership model results. | `per`: Prepared person table.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br>&bull; `transit_pass_ownership` (`Boolean-like`): Whether the person owns a transit pass.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `person_type` (`String`): person-type code or rollup.<br>`transit_pass_ownership_status` (`String`): `has_transit_pass` or `no_transit_pass`.<br>`person_type_label` (`String`): configured readable person-type label.<br>`person_count` (`Float64`): weighted people in the group. | `processor.summarize.summaries.long_term_person.transit_pass`<br>Default build: **yes** |
| `transit_subsidy_by_person_type`<br>`transit_subsidy_by_person_type.csv` | Transit-pass subsidy alternatives for workers, by person type and with an all-person-types rollup. Use it to examine employer or institutional transit-benefit assumptions. | `per`: Prepared person table.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br>&bull; `transit_pass_subsidy` (`category`): Modeled transit-pass-subsidy alternative.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `is_student` (`Boolean-like`): Whether the person is classified as a student.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `person_type` (`String`): person-type code or rollup.<br>`transit_subsidy_status` (`String`): prepared subsidy alternative code.<br>`transit_subsidy_label` (`String`): configured readable subsidy label.<br>`person_type_label` (`String`): configured readable person-type label.<br>`person_count` (`Float64`): weighted eligible workers in the group. | `processor.summarize.summaries.long_term_person.transit_subsidy`<br>Default build: **yes** |
| `telecommute_frequency_distribution`<br>`telecommute_frequency_distribution.csv` | Non-work-from-home workers by telecommute-frequency category and home geography, plus an all-geographies rollup. Use it to analyze recurring telecommuting among workers who still have an external workplace. | `per`: Prepared person table.<br>&bull; `telecommute_frequency` (`category`): Modeled telecommute-frequency alternative.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `work_from_home` (`String`): Whether the worker has a home workplace.<br>&bull; `home_zone_id` (`identifier`): Prepared home-zone identifier. | `geography_type` (`String`): home-geography system or rollup.<br>`geography_id` (`String`): home-geography identifier or rollup value.<br>`telecommute_frequency` (`String`): prepared telecommute-frequency alternative.<br>`person_count` (`Float64`): weighted workers in the group. | `processor.summarize.summaries.long_term_person.telecommute`<br>Default build: **yes** |

### Household Vehicles and Vehicle Characteristics

_Related dashboard pages: Vehicle Ownership and Type. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `autonomous_vehicle_ownership_totals`<br>`autonomous_vehicle_ownership_totals.csv` | One run-level total of households modeled as owning an autonomous vehicle. Use it to report scenario penetration and compare AV-ownership assumptions. | `hh`: Prepared household table.<br>&bull; `av_ownership` (`Boolean-like`): Whether the household owns an autonomous vehicle.<br>&bull; `finalweight` (`Float64`): Household expansion weight from the configured weight, inverse sample rate, or 1.0 fallback. | `household_with_autonomous_vehicle_count` (`Float64`): weighted households with `av_ownership` true. | `processor.summarize.summaries.long_term_vehicle.av_ownership`<br>Default build: **yes** |
| `auto_ownership_distribution`<br>`auto_ownership_distribution.csv` | Household totals jointly classified by household size and vehicle count; household sizes of five or more are grouped as `5+`. Use it to assess motorization and auto sufficiency. | `hh`: Prepared household table.<br>&bull; `HHSIZE` (`Int32`): Household person count clipped to 1 through 5, where 5 represents households of five or more people.<br>&bull; `HHVEH` (`Int32`): Household vehicle count clipped to 0 through 4 for standard summary categories.<br>&bull; `finalweight` (`Float64`): Household expansion weight from the configured weight, inverse sample rate, or 1.0 fallback. | `household_size` (`String`): household-size category, with `5+` as the terminal group.<br>`household_vehicle_count` (`Int64`): vehicles available to the household.<br>`household_count` (`Float64`): weighted households in the joint category. | `processor.summarize.summaries.long_term_vehicle.auto_ownership`<br>Default build: **yes** |
| `vehicle_age_distribution`<br>`vehicle_age_distribution.csv` | Household vehicles by age, with ages 20 and older grouped as `20+`. Use it for fleet turnover, emissions, and technology analyses. | `vehicles`: Prepared household-vehicle table.<br>&bull; `vehicle_age` (`Int64`): Vehicle age in years parsed from vehicle_type.<br>&bull; `finalweight` (`Float64`): Vehicle expansion weight inherited from the owning household or set to 1.0 when unavailable. | `age` (`String`): vehicle age in years or `20+`.<br>`vehicle_count` (`Float64`): weighted vehicles in the age category. | `processor.summarize.summaries.long_term_vehicle.vehicle_char_age`<br>Default build: **yes** |
| `vehicle_fuel_type_distribution`<br>`vehicle_fuel_type_distribution.csv` | Household vehicles by prepared fuel or powertrain type. Use it for fleet composition, energy, and emissions analysis. | `vehicles`: Prepared household-vehicle table.<br>&bull; `fuel_type` (`String`): Vehicle fuel or powertrain category parsed from vehicle_type.<br>&bull; `finalweight` (`Float64`): Vehicle expansion weight inherited from the owning household or set to 1.0 when unavailable. | `fuel_type` (`String`): prepared vehicle fuel/powertrain category.<br>`vehicle_count` (`Float64`): weighted vehicles of that type. | `processor.summarize.summaries.long_term_vehicle.vehicle_char_fuel`<br>Default build: **yes** |
| `vehicle_body_type_distribution`<br>`vehicle_body_type_distribution.csv` | Household vehicles by prepared body type. Use it to characterize the light-duty fleet and support occupancy or emissions comparisons. | `vehicles`: Prepared household-vehicle table.<br>&bull; `body_type` (`String`): Vehicle body-style category parsed from vehicle_type.<br>&bull; `finalweight` (`Float64`): Vehicle expansion weight inherited from the owning household or set to 1.0 when unavailable. | `body_type` (`String`): prepared vehicle body-style category.<br>`vehicle_count` (`Float64`): weighted vehicles of that type. | `processor.summarize.summaries.long_term_vehicle.vehicle_char_body`<br>Default build: **yes** |

### Long-Term Geography, Location, and Shadow Pricing

_Related dashboard pages: Mandatory Location Choice, Employment\Enrollment Match By Geography, Park-and-Ride Location, Regional Validation. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `work_from_home_rate_by_geography`<br>`work_from_home_rate_by_geography.csv` | All workers and work-from-home workers by home geography, plus a regional rollup. Divide the WFH count by the worker count to calculate the WFH rate and map its spatial pattern. | `per`: Prepared person table.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `home_zone_id` (`identifier`): Prepared home-zone identifier.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `geography_type` (`String`): home-geography system or rollup.<br>`geography_id` (`String`): home-geography identifier or rollup value.<br>`worker_count` (`Float64`): weighted workers living in the geography.<br>`work_from_home_worker_count` (`Float64`): weighted workers flagged as working from home. | `processor.summarize.summaries.long_term_geography.wfh`<br>Default build: **yes** |
| `internal_external_worker_by_geography`<br>`internal_external_worker_by_geography.csv` | Internal and external workers by home geography, plus a regional rollup. Use it to understand external-worker incidence and its residential distribution. | `per`: Prepared person table.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `is_external_worker` (`Boolean-like`): Whether the person's assigned workplace is external to the modeled area.<br>&bull; `home_zone_id` (`identifier`): Prepared home-zone identifier.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `geography_type` (`String`): home-geography system or rollup.<br>`geography_id` (`String`): home-geography identifier or rollup value.<br>`internal_worker_count` (`Float64`): weighted workers with an internal workplace.<br>`external_worker_count` (`Float64`): weighted workers classified as external. | `processor.summarize.summaries.long_term_geography.internal_vs_external`<br>Default build: **yes** |
| `external_worker_workplace_locations`<br>`external_worker_workplace_locations.csv` | External workers by their external workplace geography, plus a regional rollup. Use it to analyze external commute orientation; the all-worker denominator is repeated to support share calculations. | `per`: Prepared person table.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `is_external_worker` (`Boolean-like`): Whether the person's assigned workplace is external to the modeled area.<br>&bull; `external_workplace_zone_id` (`identifier`): Assigned external workplace zone identifier.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `geography_type` (`String`): external-workplace geography system or rollup.<br>`geography_id` (`String`): external-workplace geography identifier or rollup value.<br>`external_worker_count` (`Float64`): weighted external workers assigned to that destination.<br>`all_worker_count` (`Float64`): weighted workers in the full run, repeated on every row. | `processor.summarize.summaries.long_term_geography.external_workplace_loc`<br>Default build: **yes** |
| `workplace_location_employment_comparison`<br>`workplace_location_employment_comparison.csv` | Land-use employment and modeled worker workplace choices aligned by workplace geography. Use it to compare attraction targets with assigned workers and diagnose location-choice balance. | `land_use`: Prepared land-use and zone-attribute table.<br>&bull; `MAZ` (`Int64`): Canonical micro-analysis-zone identifier.<br>&bull; `employment_count` (`Float64`): Employment opportunities associated with the zone.<br><br>`per`: Prepared person table.<br>&bull; `workplace_zone_id` (`identifier`): Assigned internal workplace-zone identifier.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `geography_type` (`String`): workplace-geography system.<br>`geography_id` (`String`): workplace-geography identifier.<br>`employment_count` (`Float64`): employment opportunities from land use.<br>`worker_count` (`Float64`): weighted workers assigned to the geography. | `processor.summarize.summaries.long_term_geography.workplace_vs_land_use_employment`<br>Default build: **yes** |
| `commuting_flows`<br>`commuting_flows.csv` | Worker flows from home geography to workplace geography at matching configured geography levels, plus a regional total. Use it as an origin-destination matrix for commute sheds, self-containment, and interjurisdictional flows. | `per`: Prepared person table.<br>&bull; `home_zone_id` (`identifier`): Prepared home-zone identifier.<br>&bull; `workplace_zone_id` (`identifier`): Assigned internal workplace-zone identifier.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `origin_geography_type` (`String`): home-geography system.<br>`origin_geography_id` (`String`): home-geography identifier.<br>`destination_geography_type` (`String`): workplace-geography system.<br>`destination_geography_id` (`String`): workplace-geography identifier.<br>`commuter_count` (`Float64`): weighted workers in the OD pair. | `processor.summarize.summaries.long_term_geography.commuting_flows`<br>Default build: **yes** |
| `school_location_enrollment_comparison`<br>`school_location_enrollment_comparison.csv` | Land-use enrollment and modeled student school locations aligned by geography and student type. Use it to compare school-location targets with assigned students. | `land_use`: Prepared land-use and zone-attribute table.<br>&bull; `MAZ` (`Int64`): Canonical micro-analysis-zone identifier.<br>&bull; `enrollment_count` (`Float64`): Enrollment opportunities associated with the zone and student type.<br>&bull; `student_type` (`String`): Student or enrollment market segment.<br><br>`per`: Prepared person table.<br>&bull; `school_zone_id` (`identifier`): Assigned school-zone identifier.<br>&bull; `is_student` (`Boolean-like`): Whether the person is classified as a student.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `geography_type` (`String`): school-geography system.<br>`geography_id` (`String`): school-geography identifier.<br>`student_type` (`String`): prepared school/enrollment market segment.<br>`enrollment_count` (`Float64`): target enrollment from land use.<br>`student_count` (`Float64`): weighted students assigned to the geography and type. | `processor.summarize.summaries.long_term_geography.school_loc_vs_land_use_enrollment`<br>Default build: **yes** |
| `workplace_shadow_pricing_residuals`<br>`workplace_shadow_pricing_residuals.csv` | Zone-level workplace target-versus-modeled residuals. Use positive residuals to find over-assigned workplace geographies and negative residuals to find under-assigned ones. | `land_use`: Prepared land-use and zone-attribute table.<br>&bull; `MAZ` (`Int64`): Canonical micro-analysis-zone identifier.<br>&bull; `employment_count` (`Float64`): Employment opportunities associated with the zone.<br><br>`per`: Prepared person table.<br>&bull; `workplace_zone_id` (`identifier`): Assigned internal workplace-zone identifier.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `geography_type` (`String`): workplace-geography system.<br>`geography_id` (`String`): workplace-geography identifier.<br>`target_count` (`Float64`): land-use employment target.<br>`modeled_count` (`Float64`): weighted assigned workers.<br>`residual_count` (`Float64`): `modeled_count - target_count`.<br>`absolute_residual_count` (`Float64`): absolute residual magnitude.<br>`percent_error` (`Float64`): residual divided by target, times 100; null when target is zero. | `processor.summarize.summaries.long_term_geography.workplace_shadow_pricing_residuals`<br>Default build: **yes** |
| `school_shadow_pricing_residuals`<br>`school_shadow_pricing_residuals.csv` | Zone-level school target-versus-modeled residuals by student type. Use it to diagnose school-location shadow-pricing convergence and segment-specific imbalance. | `land_use`: Prepared land-use and zone-attribute table.<br>&bull; `MAZ` (`Int64`): Canonical micro-analysis-zone identifier.<br>&bull; `enrollment_count` (`Float64`): Enrollment opportunities associated with the zone and student type.<br>&bull; `student_type` (`String`): Student or enrollment market segment.<br><br>`per`: Prepared person table.<br>&bull; `school_zone_id` (`identifier`): Assigned school-zone identifier.<br>&bull; `is_student` (`Boolean-like`): Whether the person is classified as a student.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br>&bull; `student_type` (`String`): Prepared school or enrollment market segment. | `geography_type` (`String`): school-geography system.<br>`geography_id` (`String`): school-geography identifier.<br>`student_type` (`String`): school/enrollment market segment.<br>`target_count` (`Float64`): land-use enrollment target.<br>`modeled_count` (`Float64`): weighted assigned students.<br>`residual_count` (`Float64`): modeled minus target.<br>`absolute_residual_count` (`Float64`): absolute residual magnitude.<br>`percent_error` (`Float64`): residual divided by target, times 100; null for zero targets. | `processor.summarize.summaries.long_term_geography.school_shadow_pricing_residuals`<br>Default build: **yes** |
| `workplace_shadow_pricing_residual_histogram`<br>`workplace_shadow_pricing_residual_histogram.csv` | Distribution of workplace residuals by geography system. Use it to assess convergence across all zones without inspecting each zone separately; zero residuals receive their own zero-width bin. | `land_use`: Prepared land-use and zone-attribute table.<br>&bull; `MAZ` (`Int64`): Canonical micro-analysis-zone identifier.<br>&bull; `employment_count` (`Float64`): Employment opportunities associated with the zone.<br><br>`per`: Prepared person table.<br>&bull; `workplace_zone_id` (`identifier`): Assigned internal workplace-zone identifier.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `geography_type` (`String`): workplace-geography system.<br>`bin_start` (`Float64`): inclusive lower residual bound.<br>`bin_end` (`Float64`): upper residual bound; both bounds are zero for the exact-zero bin.<br>`geography_count` (`Float64`): number of geography records in the bin. | `processor.summarize.summaries.long_term_geography.workplace_shadow_pricing_residual_histogram`<br>Default build: **yes** |
| `school_shadow_pricing_residual_histogram`<br>`school_shadow_pricing_residual_histogram.csv` | Distribution of school residuals by geography system and student type. Use it to compare convergence across student markets. | `land_use`: Prepared land-use and zone-attribute table.<br>&bull; `MAZ` (`Int64`): Canonical micro-analysis-zone identifier.<br>&bull; `enrollment_count` (`Float64`): Enrollment opportunities associated with the zone and student type.<br>&bull; `student_type` (`String`): Student or enrollment market segment.<br><br>`per`: Prepared person table.<br>&bull; `school_zone_id` (`identifier`): Assigned school-zone identifier.<br>&bull; `is_student` (`Boolean-like`): Whether the person is classified as a student.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br>&bull; `student_type` (`String`): Prepared school or enrollment market segment. | `geography_type` (`String`): school-geography system.<br>`student_type` (`String`): school/enrollment market segment.<br>`bin_start` (`Float64`): lower residual bound.<br>`bin_end` (`Float64`): upper residual bound, or zero for the exact-zero bin.<br>`geography_count` (`Float64`): number of geography/student-type records in the bin. | `processor.summarize.summaries.long_term_geography.school_shadow_pricing_residual_histogram`<br>Default build: **yes** |
| `park_and_ride_location_residuals`<br>`park_and_ride_location_residuals.csv` | Modeled park-and-ride tour use compared with lot capacity by lot geography. Use it to identify over-capacity or underused PNR locations. | `tours`: Prepared tour table.<br>&bull; `tour_mode` (`String`): Main mode selected for the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights.<br><br>`land_use`: Prepared land-use and zone-attribute table. | `geography_type` (`String`): PNR-lot geography system.<br>`geography_id` (`String`): PNR-lot geography identifier.<br>`pnr_tour_count` (`Float64`): weighted PNR tours assigned to the location.<br>`pnr_lot_capacity` (`Float64`): supplied lot capacity target.<br>`residual_count` (`Float64`): tours minus capacity.<br>`absolute_residual_count` (`Float64`): absolute residual magnitude.<br>`percent_error` (`Float64`): residual divided by capacity, times 100; null for zero capacity. | `processor.summarize.summaries.long_term_geography.park_and_ride_location_residuals`<br>Default build: **yes** |
| `park_and_ride_location_residual_histogram`<br>`park_and_ride_location_residual_histogram.csv` | Distribution of PNR use-minus-capacity residuals by geography system. Use it for systemwide capacity-fit assessment. | `tours`: Prepared tour table.<br>&bull; `tour_mode` (`String`): Main mode selected for the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights.<br><br>`land_use`: Prepared land-use and zone-attribute table. | `geography_type` (`String`): PNR-lot geography system.<br>`bin_start` (`Float64`): lower residual bound.<br>`bin_end` (`Float64`): upper residual bound, or zero for the exact-zero bin.<br>`geography_count` (`Float64`): number of PNR geography records in the bin. | `processor.summarize.summaries.long_term_geography.park_and_ride_location_residual_histogram`<br>Default build: **yes** |
| `free_parking_eligibility_by_workplace_geography`<br>`free_parking_eligibility_by_workplace_geography.csv` | Workers with and without free workplace parking by workplace geography. Use it to analyze parking-cost exposure and its effect on commute mode choice. | `per`: Prepared person table.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `free_parking_at_work` (`Boolean-like or category`): Free-workplace-parking eligibility or modeled alternative.<br>&bull; `workplace_zone_id` (`identifier`): Assigned internal workplace-zone identifier.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `geography_type` (`String`): workplace-geography system.<br>`geography_id` (`String`): workplace-geography identifier.<br>`workers_without_free_parking_count` (`Float64`): weighted workers not eligible for free parking.<br>`workers_with_free_parking_count` (`Float64`): weighted workers eligible for free parking. | `processor.summarize.summaries.long_term_geography.free_parking`<br>Default build: **yes** |

### Long-Term Location Distance

_Related dashboard pages: Mandatory Location Choice. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `work_location_distance_distribution_by_geography`<br>`work_location_distance_distribution_by_geography.csv` | Internal, non-work-from-home workers with valid workplace locations by home geography and home-to-work distance. Use it for commute-length distributions and spatial comparisons. | `per`: Prepared person table.<br>&bull; `distance_to_work` (`Float64`): Prepared home-to-work distance in the configured distance units.<br>&bull; `external_workplace_zone_id` (`identifier`): Assigned external workplace zone identifier.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br>&bull; `is_external_worker` (`Boolean-like`): Whether the person's assigned workplace is external to the modeled area.<br>&bull; `is_worker` (`String`): Whether the person is classified as a worker.<br>&bull; `work_from_home` (`String`): Whether the worker has a home workplace.<br>&bull; `workplace_zone_id` (`identifier`): Assigned internal workplace-zone identifier. | `distance_bin` (`String`): exact-zero, positive-sub-mile, whole-mile lower-bound, or `51+` category.<br>`geography_type` (`String`): home-geography system or rollup.<br>`geography_id` (`String`): home-geography identifier or rollup value.<br>`person_count` (`Float64`): weighted workers in the bin. | `processor.summarize.summaries.long_term_distance.work_tlfd`<br>Default build: **yes** |
| `university_location_distance_distribution_by_geography`<br>`university_location_distance_distribution_by_geography.csv` | University students, identified by person type 3, by home geography and home-to-school distance. Use it to examine university travel markets and campus catchments. | `per`: Prepared person table.<br>&bull; `distance_to_school` (`Float64`): Prepared home-to-school distance in the configured distance units.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `distance_bin` (`String`): exact-zero, positive-sub-mile, whole-mile lower-bound, or `51+` category.<br>`geography_type` (`String`): home-geography system or rollup.<br>`geography_id` (`String`): home-geography identifier or rollup value.<br>`person_count` (`Float64`): weighted university students in the bin. | `processor.summarize.summaries.long_term_distance.univ_tlfd`<br>Default build: **yes** |
| `school_location_distance_distribution_by_geography`<br>`school_location_distance_distribution_by_geography.csv` | School students, identified by person types 6 and higher, by home geography and home-to-school distance. Use it for K--12 travel-distance and school-catchment analysis. | `per`: Prepared person table.<br>&bull; `distance_to_school` (`Float64`): Prepared home-to-school distance in the configured distance units.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `distance_bin` (`String`): exact-zero, positive-sub-mile, whole-mile lower-bound, or `51+` category.<br>`geography_type` (`String`): home-geography system or rollup.<br>`geography_id` (`String`): home-geography identifier or rollup value.<br>`person_count` (`Float64`): weighted school students in the bin. | `processor.summarize.summaries.long_term_distance.schl_tlfd`<br>Default build: **yes** |

### Daily Activity Patterns and Travel Rates

_Related dashboard pages: Daily Activity Pattern. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `daily_activity_pattern_by_person_type`<br>`daily_activity_pattern_by_person_type.csv` | Daily activity pattern alternatives by person type, using each person's non-null person-table CDAP value and falling back only for remaining persons to all observed day rows with day weights when available, including an all-person-types rollup. No day rows are deduplicated. Use it to compare mandatory, nonmandatory, and home-stay behavior. | `per`: Prepared person table.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `person_type` (`String`): person-type code or rollup.<br>`daily_activity_pattern` (`String`): prepared CDAP/activity-pattern category.<br>`person_count` (`Float64`): weighted people or observed person-days in the pattern. | `processor.summarize.summaries.daily_travel_activity.dap_summary`<br>Default build: **yes** |
| `mandatory_tour_frequency_by_person_type`<br>`mandatory_tour_frequency_by_person_type.csv` | Positive mandatory-tour-frequency choice by person type, using each person's non-null prepared choice and falling back only for remaining persons to choices derived from work and school tours on every person-day, with day weights when available, plus an all-person-types rollup. Use it to analyze how many mandatory tours travelers make. The table excludes records with a choice of zero. | `per`: Prepared person table.<br>&bull; `person_id` (`Int64`): Canonical person identifier.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback. | `person_type` (`String`): person-type code or rollup.<br>`mandatory_tour_frequency` (`Int32`): prepared or tour-derived positive mandatory-tour frequency alternative.<br>`person_count` (`Float64`): weighted people or person-days choosing that frequency. | `processor.summarize.summaries.daily_travel_activity.mandatory_tour_freq`<br>Default build: **yes** |
| `nonmandatory_tour_frequency_by_person_type`<br>`nonmandatory_tour_frequency_by_person_type.csv` | Count of individual nonmandatory tours plus joint-tour participation, grouped as 0, 1, 2, or 3+, by person type and for all types. Genuinely multi-day inputs are classified per person-day only when every tour and participant has a safe day key; otherwise the one-day/person fallback is retained rather than dropping records or guessing keys. Use it to compare discretionary travel propensity. | `per`: Prepared person table.<br>&bull; `person_id` (`Int64`): Canonical person identifier.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br><br>`tours`: Prepared tour table.<br>&bull; `person_id` (`Int64`): Canonical person identifier associated with an individual tour.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br><br>`joint_participants`: Prepared joint-tour participant table.<br>&bull; `person_id` (`Int64`): Canonical identifier of a person participating in a joint tour. | `person_type` (`String`): person-type code or rollup.<br>`nonmandatory_tour_frequency` (`String`): combined nonmandatory-tour category `0`, `1`, `2`, or `3+`.<br>`person_count` (`Float64`): weighted people or person-days in the category. | `processor.summarize.summaries.daily_travel_activity.indiv_nm_summary`<br>Default build: **yes** |
| `tour_rates_by_person_type_and_tour_purpose`<br>`tour_rates_by_person_type_and_tour_purpose.csv` | Person-tours per person-day by person type and tour purpose under the selected weighting mode, plus all-person-types rates. Every eligible source row contributes and at-work subtours are excluded. Joint tours are attributed using each known participant's person weight or expanded by a valid party size when participant records are unavailable. Use it to compare tour-generation rates while controlling for population composition. | `per`: Prepared person table.<br>&bull; `person_id` (`Int64`): Canonical person identifier.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br><br>`tours`: Prepared tour table.<br>&bull; `person_id` (`Int64`): Canonical person identifier associated with an individual tour.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `person_type` (`String`): person-type code or rollup.<br>`tour_purpose` (`String`): prepared tour-purpose category.<br>`tour_rate` (`Float64`): sum of eligible person-tour weights, divided by summed person or person-day weights for that person type. | `processor.summarize.summaries.daily_travel_activity.tour_rate_per_person`<br>Default build: **yes** |
| `trip_rates_by_person_type_and_trip_purpose`<br>`trip_rates_by_person_type_and_trip_purpose.csv` | Person-trips per person-day by person type and trip purpose under the selected weighting mode, plus all-person-types rates. Every eligible source row contributes. Joint trips are attributed using each known participant's person weight or expanded by a valid party size when participant records are unavailable. Use it to compare trip-generation rates across demographic markets. | `per`: Prepared person table.<br>&bull; `person_id` (`Int64`): Canonical person identifier.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br><br>`trips`: Prepared trip table.<br>&bull; `person_id` (`Int64`): Canonical person identifier associated with the trip.<br>&bull; `trip_purpose` (`String`): Destination purpose of the trip leg.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `person_type` (`String`): person-type code or rollup.<br>`trip_purpose` (`String`): destination purpose of the trip.<br>`trip_rate` (`Float64`): sum of eligible person-trip weights, divided by summed person or person-day weights for that person type. | `processor.summarize.summaries.daily_travel_activity.trip_rate_per_person`<br>Default build: **yes** |

### School Escorting

_Related dashboard pages: Escorted Tours. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `escorted_tour_totals`<br>`escorted_tour_totals.csv` | One run-level total of adult-side tours with an outbound or inbound school-escort condition. Use it as the top-level escorted-tour control total. | `tours`: Prepared tour table.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `tour_count` (`Float64`): weighted distinct eligible tours with at least one escorted direction. | `processor.summarize.summaries.daily_travel_escort_counts.total_escorted_tours`<br>Default build: **yes** |
| `school_escorted_tours_by_escort_type_and_direction`<br>`school_escorted_tours_by_escort_type_and_direction.csv` | Adult-side escorted tours by escort arrangement and direction, with an `all_directions` incidence rollup. Use it to compare ride-share and pure-escort patterns. | `tours`: Prepared tour table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `escort_type` (`String`): prepared escort arrangement label.<br>`direction` (`String`): `outbound`, `inbound`, or `all_directions`.<br>`tour_count` (`Float64`): weighted escorted-tour incidences. | `processor.summarize.summaries.daily_travel_escort_counts.escorted_tours_to_from_school`<br>Default build: **yes** |
| `adult_escorted_tour_purposes_by_direction`<br>`adult_escorted_tour_purposes_by_direction.csv` | Purposes of the adult tours that do school escorting, by direction and with an all-directions incidence rollup. Use it to see how escorting connects with work or other adult activities. | `tours`: Prepared tour table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `tour_purpose` (`String`): adult tour's primary purpose.<br>`direction` (`String`): escorted half or `all_directions`.<br>`tour_count` (`Float64`): weighted escorted-tour incidences. | `processor.summarize.summaries.daily_travel_escort_counts.adult_escorted_tour_purposes_by_direction`<br>Default build: **yes** |
| `adult_escorted_tours_by_person_type_and_direction`<br>`adult_escorted_tours_by_person_type_and_direction.csv` | Adult-side escorted tours by the adult traveler's person type and escorted direction. Use it to identify who performs school escorting. | `tours`: Prepared tour table.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `person_type` (`String`): adult traveler person-type code.<br>`direction` (`String`): `outbound`, `inbound`, or `both`.<br>`tour_count` (`Float64`): weighted tours meeting that directional condition. | `processor.summarize.summaries.daily_travel_escort_counts.adult_escorted_tours_by_person_type_and_direction`<br>Default build: **yes** |
| `student_school_escort_status_by_direction`<br>`student_school_escort_status_by_direction.csv` | Student school tours classified by normalized escort type for each direction and for tours escorted both ways. Use it to measure the student-side escort experience. | `tours`: Prepared tour table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `direction` (`String`): `outbound`, `inbound`, or `both`.<br>`escort_type` (`String`): normalized escort arrangement, including unescorted alternatives where present.<br>`tour_count` (`Float64`): weighted student school tours in the group. | `processor.summarize.summaries.daily_travel_escort_counts.student_school_escort_status_by_direction`<br>Default build: **yes** |
| `student_households_by_student_count`<br>`student_households_by_student_count.csv` | Households by the number of school-age/student household members recognized by the escort logic. Use it as a denominator for household escort participation. | `hh`: Prepared household table.<br>&bull; `household_id` (`Int64`): Canonical household identifier used to join people, tours, trips, days, and vehicles.<br>&bull; `finalweight` (`Float64`): Household expansion weight from the configured weight, inverse sample rate, or 1.0 fallback.<br><br>`per`: Prepared person table.<br>&bull; `household_id` (`Int64`): Canonical household identifier for the person.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code. | `student_count` (`Int64`): students in the household.<br>`household_count` (`Float64`): weighted households with that count. | `processor.summarize.summaries.daily_travel_escort_counts.student_households_by_student_count`<br>Default build: **yes** |
| `households_with_school_escorting_by_student_count_and_direction`<br>`households_with_school_escorting_by_student_count_and_direction.csv` | Unique households with at least one escorted student school tour, by number of students and directional condition. Use it to calculate escort-participation rates by household composition. | `hh`: Prepared household table.<br>&bull; `household_id` (`Int64`): Canonical household identifier used to join people, tours, trips, days, and vehicles.<br>&bull; `finalweight` (`Float64`): Household expansion weight from the configured weight, inverse sample rate, or 1.0 fallback.<br><br>`per`: Prepared person table.<br>&bull; `household_id` (`Int64`): Canonical household identifier for the person.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br><br>`tours`: Prepared tour table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `student_count` (`Int64`): students in the household.<br>`direction` (`String`): `outbound`, `inbound`, or `both`.<br>`household_count` (`Float64`): weighted unique households meeting the condition. | `processor.summarize.summaries.daily_travel_escort_counts.households_with_school_escorting_by_student_count_and_direction`<br>Default build: **yes** |
| `schoolkids_per_escorted_tour_by_student_count_and_direction`<br>`schoolkids_per_escorted_tour_by_student_count_and_direction.csv` | Average number of escorted children on adult-side escorted tours by household student count and direction. Use it to analyze escorting efficiency and child grouping. | `hh`: Prepared household table.<br>&bull; `household_id` (`Int64`): Canonical household identifier used to join people, tours, trips, days, and vehicles.<br>&bull; `finalweight` (`Float64`): Household expansion weight from the configured weight, inverse sample rate, or 1.0 fallback.<br><br>`per`: Prepared person table.<br>&bull; `household_id` (`Int64`): Canonical household identifier for the person.<br>&bull; `person_type` (`source-compatible category`): ActivitySim person-type code.<br><br>`tours`: Prepared tour table.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br>&bull; `num_escortees` (`Int64`): Number of students escorted on the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `student_count` (`Int64`): students in the adult traveler's household.<br>`direction` (`String`): `outbound`, `inbound`, or `both`.<br>`avg_schoolkids_per_tour` (`Float64`): weighted mean number of escortees per eligible tour.<br>`tour_count` (`Float64`): weighted eligible tours used as the mean denominator. | `processor.summarize.summaries.daily_travel_escort_counts.schoolkids_per_escorted_tour_by_student_count_and_direction`<br>Default build: **yes** |
| `adult_escorted_tour_distance_distribution_by_direction`<br>`adult_escorted_tour_distance_distribution_by_direction.csv` | Adult-side escorted tours by lower-bound tour-distance bin and directional escort condition. Use it to compare the length of outbound-only, inbound-only, and both-way escort tours. | `tours`: Prepared tour table.<br>&bull; `SKIMDIST` (`Float64`): Prepared tour distance in the configured skim distance units.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `distance_bin` (`String`): exact `0`, positive sub-mile `>0-<1`, whole-mile lower bound, or `40+`.<br>`direction` (`String`): `outbound`, `inbound`, or `both`.<br>`tour_count` (`Float64`): weighted eligible tours in the bin. | `processor.summarize.summaries.daily_travel_escort_distributions.adult_escorted_tour_distance_distribution_by_direction`<br>Default build: **yes** |
| `adult_escorted_trip_distance_distribution_by_direction`<br>`adult_escorted_trip_distance_distribution_by_direction.csv` | Trips on adult tours marked as escorted, by outbound or inbound half and lower-bound trip-distance bin. Use it to examine the trip-leg distance for escorting. | `tours`: Prepared tour table.<br>&bull; `tour_id` (`Int64`): Canonical tour identifier.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br><br>`trips`: Prepared trip table.<br>&bull; `tour_id` (`Int64`): Canonical identifier of the parent tour.<br>&bull; `od_dist` (`Float64`): Origin-to-destination trip distance from the configured distance skim or supported survey distance, in source units.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `distance_bin` (`String`): exact `0`, positive sub-mile `>0-<1`, whole-mile lower bound, or `40+`.<br>`direction` (`String`): `outbound`, `inbound`, or `both` condition.<br>`trip_count` (`Float64`): weighted eligible trips in the bin. | `processor.summarize.summaries.daily_travel_escort_distributions.adult_escorted_trip_distance_distribution_by_direction`<br>Default build: **yes** |
| `adult_escort_event_stop_distribution`<br>`adult_escort_event_stop_distribution.csv` | Intermediate stops before and after school drop-off or pickup on adult tours marked as escorted. Use it to analyze trip chains around escort events. | `tours`: Prepared tour table.<br>&bull; `tour_id` (`Int64`): Canonical tour identifier.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br><br>`trips`: Prepared trip table.<br>&bull; `tour_id` (`Int64`): Canonical identifier of the parent tour.<br>&bull; `escort_event_role` (`String`): Adult escort-event role assigned to the trip.<br>&bull; `escort_stops_before_event` (`Int32`): Stops occurring before the associated escort event.<br>&bull; `escort_stops_after_event` (`Int32`): Stops occurring after the associated escort event.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `segment` (`String`): one of `outbound_before_dropoff`, `outbound_after_dropoff`, `inbound_before_pickup`, or `inbound_after_pickup`.<br>`stop_count` (`Int32`): prepared count of stops in that segment.<br>`tour_count` (`Float64`): weighted escort-event records with that stop count. | `processor.summarize.summaries.daily_travel_escort_distributions.adult_escort_event_stop_distribution`<br>Default build: **yes** |
| `adult_escort_trip_stop_frequency`<br>`adult_escort_trip_stop_frequency.csv` | Adult-side escorted tours jointly classified by purpose and outbound, inbound, and total stop counts. Use it to compare stop-making complexity on escort tours. | `tours`: Prepared tour table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `school_esc_outbound` (`String`): Outbound school-escort alternative or status.<br>&bull; `school_esc_inbound` (`String`): Inbound school-escort alternative or status.<br>&bull; `num_ob_stops` (`Int32`): Number of outbound intermediate stops parsed from stop_frequency.<br>&bull; `num_ib_stops` (`Int32`): Number of inbound intermediate stops parsed from stop_frequency.<br>&bull; `num_tot_stops` (`Int32`): Total intermediate stops on the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `tour_purpose` (`String`): adult tour purpose.<br>`outbound_stop_count` (`Int32`): outbound stops capped at 3.<br>`inbound_stop_count` (`Int32`): inbound stops capped at 3.<br>`total_stop_count` (`Int32`): total stops capped at 6.<br>`tour_count` (`Float64`): weighted escorted tours in the combination. | `processor.summarize.summaries.daily_travel_escort_distributions.adult_escort_trip_stop_frequency`<br>Default build: **yes** |

### Joint Travel

_Related dashboard pages: Joint Travel. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `jtf_distribution`<br>`jtf_distribution.csv` | Household observations across the legacy 21 joint-tour-frequency labels, selected by minimum counts in five fixed purpose slots covering shopping, maintenance, eating out, visiting/social, and other discretionary. Purposes outside those slots do not affect the label, and counts above a label's threshold are not rejected, so the labels are not strict total-tour counts. When the day table shows a genuinely multi-day diary and tours contain `day_num`, observations use `(household_id, day_num)`; null or unmatched tour days do not contribute. Without the tour day column, the result is empty rather than pooled into a weekly choice. On a one-day input, the declaration does not require tours, so an unavailable tour table can instead classify every household as `No Joint Tours`; verify tour availability before using that result. Use it to validate compatible joint-tour generation inputs. | `hh`: Prepared household table.<br>&bull; `household_id` (`Int64`): Canonical household identifier used to join people, tours, trips, days, and vehicles.<br>&bull; `finalweight` (`Float64`): Household expansion weight from the configured weight, inverse sample rate, or 1.0 fallback. | `jtf_code` (`Int32`): integer alternative 1--21.<br>`jtf_label` (`String`): readable minimum-frequency/purpose-combination label.<br>`household_count` (`Float64`): sum of `hh.finalweight` across households or household-day observations assigned to the alternative. | `processor.summarize.summaries.joint_travel.joint_tour_freq`<br>Default build: **yes** |
| `joint_tours_by_household_size`<br>`joint_tours_by_household_size.csv` | All household observations and observations making at least one joint tour by household size. A genuinely multi-day diary uses household-days when tours contain `day_num`; null or unmatched tour days do not contribute, and a missing tour day column produces an empty result rather than pooling days. Valid shared joint-tour or tour IDs collapse repeated records; records without either valid ID remain distinct. Use the two counts to calculate joint-tour participation rates. | `tours`: Prepared tour table.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `household_id` (`Int64`): Canonical household identifier associated with the tour.<br><br>`hh`: Prepared household table.<br>&bull; `household_id` (`Int64`): Canonical household identifier used to join people, tours, trips, days, and vehicles.<br>&bull; `HHSIZE` (`Int32`): Household person count clipped to 1 through 5, where 5 represents households of five or more people.<br>&bull; `finalweight` (`Float64`): Household expansion weight from the configured weight, inverse sample rate, or 1.0 fallback. | `household_size` (`Int32`): number of household members.<br>`household_count` (`Float64`): sum of `hh.finalweight` across households or household-day observations of that size.<br>`joint_tour_hh_count` (`Float64`): sum of `hh.finalweight` for those observations with a joint tour. | `processor.summarize.summaries.joint_travel.joint_tours_hhsize`<br>Default build: **yes** |
| `joint_tour_party_size_distribution`<br>`joint_tour_party_size_distribution.csv` | Household joint tours by valid participant count, with parties of five or more stored in bin 5. Verified survey participant-row groups retain every row at a fractional representation weight that sums to one household tour; ordinary model joint-tour rows retain full weight. Sentinel values of 995 or greater, missing values, and nonpositive values are excluded. Use it to assess joint-tour occupancy. | `tours`: Prepared tour table.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `NUMBER_HH` (`Int32`): Joint-party size from participant rows, number_of_participants, an existing NUMBER_HH value, or a fallback of one.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `party_size` (`Int32`): household participants; value 5 represents `5+`.<br>`joint_tour_count` (`Float64`): weighted joint tours in the party-size bin. | `processor.summarize.summaries.joint_travel.joint_party_size`<br>Default build: **yes** |
| `joint_tour_composition_distribution`<br>`joint_tour_composition_distribution.csv` | Household joint tours by prepared party-composition category. Verified participant-row representations retain every row fractionally so each complete group contributes one household tour. Use it to compare adult-only, child-inclusive, and other modeled compositions. | `tours`: Prepared tour table.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `tour_composition` (`String`): prepared joint-party composition.<br>`joint_tour_count` (`Float64`): weighted joint tours in the category. | `processor.summarize.summaries.joint_travel.joint_composition`<br>Default build: **yes** |
| `joint_tour_composition_by_party_size`<br>`joint_tour_composition_by_party_size.csv` | Household joint tours jointly classified by party composition and exact valid participant count. Verified participant-row representations retain every row fractionally so each complete group contributes one household tour. Sentinel values of 995 or greater, missing values, and nonpositive values are excluded. Use it to study how household makeup and group size interact. | `tours`: Prepared tour table.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `tour_composition` (`String`): prepared party-composition category.<br>`party_size` (`Int64`): number of tour participants.<br>`joint_tour_count` (`Float64`): weighted joint tours in the combination. | `processor.summarize.summaries.joint_travel.joint_composition_by_party_size`<br>Default build: **yes** |
| `person_jtp_by_household_size`<br>`person_jtp_by_household_size.csv` | All person observations and observations participating in one or more joint tours by household size. When a multi-day input has person-day records and each joint-tour identity maps to one day, the observations are person-days and participation comes from linked joint participants. A missing or ambiguous tour-day mapping produces an empty result; if the day table lacks household, person, or day fields, the person-level fallback remains. The multi-day path's participant-table limitations are documented in chapter 25. Use the two counts to calculate participation rates. | `per`: Prepared person table.<br>&bull; `household_id` (`Int64`): Canonical household identifier for the person.<br>&bull; `person_id` (`Int64`): Canonical person identifier.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br><br>`hh`: Prepared household table.<br>&bull; `household_id` (`Int64`): Canonical household identifier used to join people, tours, trips, days, and vehicles. | `household_size` (`Int64`): size of the person's household.<br>`joint_tour_person_count` (`Float64`): sum of the applicable person or day weight for people or person-days participating in a joint tour.<br>`total_person_count` (`Float64`): sum of the applicable person or day weight for people or person-days in households of that size. | `processor.summarize.summaries.joint_travel.joint_participation_person_by_hhsize`<br>Default build: **yes** |
| `household_jtp_by_household_size_and_jtf`<br>`household_jtp_by_household_size_and_jtf.csv` | For household observations of size two or more, weighted percentage distribution across 0, 1, and 2+ joint-tour identities within each household size. A genuinely multi-day diary uses household-days when tours contain `day_num`; null or unmatched tour days do not contribute, and a missing tour day column produces an empty result rather than pooling days. Valid shared joint-tour or tour IDs collapse repeated records; records without either valid ID remain distinct. Use it to compare joint-tour propensity independent of household-size totals across compatible runs. | `hh`: Prepared household table.<br>&bull; `household_id` (`Int64`): Canonical household identifier used to join people, tours, trips, days, and vehicles.<br>&bull; `HHSIZE` (`Int32`): Household person count clipped to 1 through 5, where 5 represents households of five or more people.<br>&bull; `finalweight` (`Float64`): Household expansion weight from the configured weight, inverse sample rate, or 1.0 fallback.<br><br>`tours`: Prepared tour table.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `household_id` (`Int64`): Canonical household identifier associated with the tour. | `jtf` (`String`): joint-tour count category `0`, `1`, or `2+`.<br>`household_size` (`String`): household size as a category.<br>`household_percent` (`Float64`): percent of summed `hh.finalweight` for household observations of that size in the JTF category. | `processor.summarize.summaries.joint_travel.jtf_by_hhsize`<br>Default build: **yes** |

### Basic Tour Distributions

_Related dashboard pages: Tour Purpose. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `tour_category_distribution`<br>`tour_category_distribution.csv` | Household-tour-unit counts by ActivitySim category, such as mandatory, nonmandatory, at-work, or joint. Verified participant-row representations of one joint tour are retained fractionally rather than collapsed. Use it for high-level tour-system composition. | `tours`: Prepared tour table.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `tour_category` (`String`): prepared tour category.<br>`tour_count` (`Float64`): weighted tours in the category. | `processor.summarize.summaries.tour.tour_category`<br>Default build: **yes** |
| `tour_purpose_distribution`<br>`tour_purpose_distribution.csv` | Household-tour-unit counts by configured summary purpose. Verified participant-row representations of one joint tour are retained fractionally rather than collapsed. Use it to compare the volume and share of work, school, escort, shopping, and other travel. | `tours`: Prepared tour table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `tour_purpose` (`String`): canonical summary tour purpose.<br>`tour_count` (`Float64`): weighted tours for that purpose. | `processor.summarize.summaries.tour.tour_purpose`<br>Default build: **yes** |

### Vehicles Allocated to Tours

_Related dashboard pages: Tour Mode. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `allocated_vehicle_age_by_occupancy`<br>`allocated_vehicle_age_by_occupancy.csv` | Allocated vehicle ages by occupancy condition. Use it to analyze how fleet age is associated with single- and shared-occupant travel. | `tours`: Prepared tour table.<br>&bull; `vehicle_occup_1` (`vehicle-type code`): Vehicle type allocated to single-occupant auto tours.<br>&bull; `vehicle_occup_2` (`vehicle-type code`): Vehicle type allocated to two-occupant auto tours.<br>&bull; `vehicle_occup_3.5` (`vehicle-type code`): Vehicle type allocated to auto tours with three or more occupants.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `age` (`String`): decoded vehicle age, with `20+` terminal.<br>`occupancy` (`String`): allocation condition `1`, `2`, or `3+`.<br>`vehicle_count` (`Float64`): weighted tour allocation incidences. | `processor.summarize.summaries.tour_vehicles.allocated_vehicle_age`<br>Default build: **yes** |
| `allocated_vehicle_fuel_type_by_occupancy`<br>`allocated_vehicle_fuel_type_by_occupancy.csv` | Allocated vehicle fuel/powertrain type by occupancy condition. Use it for energy or emissions segmentation of auto travel. | `tours`: Prepared tour table.<br>&bull; `vehicle_occup_1` (`vehicle-type code`): Vehicle type allocated to single-occupant auto tours.<br>&bull; `vehicle_occup_2` (`vehicle-type code`): Vehicle type allocated to two-occupant auto tours.<br>&bull; `vehicle_occup_3.5` (`vehicle-type code`): Vehicle type allocated to auto tours with three or more occupants.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `fuel_type` (`String`): decoded fuel/powertrain category.<br>`occupancy` (`String`): allocation condition `1`, `2`, or `3+`.<br>`vehicle_count` (`Float64`): weighted tour allocation incidences. | `processor.summarize.summaries.tour_vehicles.allocated_vehicle_fuel`<br>Default build: **yes** |
| `allocated_vehicle_body_type_by_occupancy`<br>`allocated_vehicle_body_type_by_occupancy.csv` | Allocated vehicle body type by occupancy condition. Use it to relate party size to the modeled vehicle used. | `tours`: Prepared tour table.<br>&bull; `vehicle_occup_1` (`vehicle-type code`): Vehicle type allocated to single-occupant auto tours.<br>&bull; `vehicle_occup_2` (`vehicle-type code`): Vehicle type allocated to two-occupant auto tours.<br>&bull; `vehicle_occup_3.5` (`vehicle-type code`): Vehicle type allocated to auto tours with three or more occupants.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `body_type` (`String`): decoded vehicle body-style category.<br>`occupancy` (`String`): allocation condition `1`, `2`, or `3+`.<br>`vehicle_count` (`Float64`): weighted tour allocation incidences. | `processor.summarize.summaries.tour_vehicles.allocated_vehicle_body`<br>Default build: **yes** |

### Tour Mode, Stops, Time, and Distance

_Related dashboard pages: Tour Mode, Tour Time, Tour Distance, Tour Stop Frequency. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `tour_mode_by_tour_purpose_and_auto_sufficiency`<br>`tour_mode_by_tour_purpose_and_auto_sufficiency.csv` | Person-tour mode counts by purpose and household auto sufficiency, with all-purpose rows. Joint records use the sum of all matching participant person weights when available and valid party-size expansion only as a fallback; sentinel, missing, or nonpositive fallback sizes resolve to one. Use it to compare mode choice across vehicle-availability markets. | `tours`: Prepared tour table.<br>&bull; `tour_mode` (`String`): Main mode selected for the tour.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `NUMBER_HH` (`Int32`): Joint-party size from participant rows, number_of_participants, an existing NUMBER_HH value, or a fallback of one.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights.<br>&bull; `AUTOSUFF` (`Int32`): Prepared household auto-sufficiency category for the tour. | `tour_mode` (`String`): prepared tour mode.<br>`tour_purpose` (`String`): tour purpose or `all_tour_purposes`.<br>`tour_count_zero_auto` (`Float64`): weighted person-tours from zero-auto households.<br>`tour_count_auto_deficient` (`Float64`): weighted person-tours from households with fewer autos than workers.<br>`tour_count_auto_sufficient` (`Float64`): weighted person-tours from auto-sufficient households.<br>`tour_count_all_households` (`Float64`): sum of the three auto-sufficiency counts. | `processor.summarize.summaries.tour_profiles.tour_mode`<br>Default build: **yes** |
| `tour_stop_frequency_by_tour_purpose`<br>`tour_stop_frequency_by_tour_purpose.csv` | Household-tour-unit counts jointly classified by purpose and outbound, inbound, and total intermediate-stop counts. Verified participant-row joint-tour groups retain every row at fractional weight summing to one tour. Use it to measure tour complexity and stop-generation patterns. | `tours`: Prepared tour table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `num_ob_stops` (`Int32`): Number of outbound intermediate stops parsed from stop_frequency.<br>&bull; `num_ib_stops` (`Int32`): Number of inbound intermediate stops parsed from stop_frequency.<br>&bull; `num_tot_stops` (`Int32`): Total intermediate stops on the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `tour_purpose` (`String`): canonical tour purpose.<br>`outbound_stop_count` (`Int32`): outbound stops capped at 3.<br>`inbound_stop_count` (`Int32`): inbound stops capped at 3.<br>`total_stop_count` (`Int32`): total stops capped at 6.<br>`tour_count` (`Float64`): weighted tours in the combination. | `processor.summarize.summaries.tour_profiles.stop_freq`<br>Default build: **yes** |
| `atwork_subtour_frequency_distribution`<br>`atwork_subtour_frequency_distribution.csv` | Mandatory work tours by their at-work-subtour-frequency alternative. Use it to validate subtour generation from the workplace. | `tours`: Prepared tour table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `atwork_subtour_frequency` (`category`): Modeled at-work subtour-frequency alternative.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `atwork_subtour_frequency_category` (`String`): prepared at-work subtour-frequency category.<br>`atwork_subtour_count` (`Float64`): weighted parent work tours choosing the category. | `processor.summarize.summaries.tour_profiles.at_work_sub_tour_freq`<br>Default build: **yes** |
| `tour_time_of_day_by_tour_purpose`<br>`tour_time_of_day_by_tour_purpose.csv` | Dense household-tour-unit departure, arrival, and duration profiles by tour purpose plus all-purpose totals. Every eligible row is retained; verified participant-row representations of one joint tour contribute fractionally so their combined weight equals one tour. Use it to compare scheduling and duration distributions. | `tours`: Prepared tour table.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `time_bin` (`Int32`): ActivitySim period index.<br>`tour_purpose` (`String`): tour purpose or `all_tour_purposes`.<br>`departure_tour_count` (`Float64`): weighted tours starting in the bin.<br>`arrival_tour_count` (`Float64`): weighted tours ending in the bin.<br>`duration_tour_count` (`Float64`): weighted tours whose prepared duration falls in the bin. | `processor.summarize.summaries.tour_profiles.tour_tod`<br>Default build: **yes** |
| `tour_distance_by_tour_purpose`<br>`tour_distance_by_tour_purpose.csv` | Nonmandatory, joint, and at-work household-tour-unit records by lower-bound `SKIMDIST` bin and purpose, plus all-purpose totals; mandatory tours are excluded to match the legacy distribution. The metric is fixed to prepared origin-to-primary-destination `SKIMDIST` and does not switch to `tour_distance` or generic `distance_miles`. Every row is retained, with verified participant-row joint-tour groups contributing fractional weight that sums to one tour. | `tours`: Prepared tour table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `distance_bin` (`String`): exact `0`, positive sub-mile `>0-<1`, whole-mile lower bound, or `40+`.<br>`tour_purpose` (`String`): purpose or `all_tour_purposes`.<br>`tour_count` (`Float64`): sum of eligible household-tour representation weight in the bin. | `processor.summarize.summaries.tour_profiles.tour_distance`<br>Default build: **yes** |

### Tour Geography

_Related dashboard pages: Mandatory Location Choice, Tour Distance, Internal vs. External Tours. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `average_mandatory_tour_distance_by_purpose_and_geography`<br>`average_mandatory_tour_distance_by_purpose_and_geography.csv` | Weighted average home-to-work or home-to-school distance for internal non-WFH workers, university students, and school students, by home geography and regionwide. Use it to compare mandatory destination accessibility. | `per`: Prepared person table.<br>&bull; `finalweight` (`Float64`): Person expansion weight from the configured person weight, inherited household weight, or 1.0 fallback.<br>&bull; `home_zone_id` (`identifier`): Prepared home-zone identifier. | `mandatory_tour_purpose` (`String`): `work`, `university`, or `school`.<br>`geography_type` (`String`): home-geography system or rollup.<br>`geography_id` (`String`): home-geography identifier or rollup.<br>`average_tour_distance` (`Float64`): mean person-level mandatory distance weighted by `per.finalweight`.<br>`person_count` (`Float64`): sum of `per.finalweight` for people contributing to the mean. | `processor.summarize.summaries.tour_geography.avg_mand_tour_distance`<br>Default build: **yes** |
| `average_nonmandatory_tour_distance_by_purpose_and_geography`<br>`average_nonmandatory_tour_distance_by_purpose_and_geography.csv` | Weighted average prepared origin-to-primary-destination `SKIMDIST` for individual nonmandatory tours by purpose and traveler home geography, plus regional rows. The metric does not switch to `tour_distance` or generic `distance_miles`. Use it to compare discretionary travel reach. | `per`: Prepared person table.<br>&bull; `person_id` (`Int64`): Canonical person identifier.<br>&bull; `home_zone_id` (`identifier`): Prepared home-zone identifier.<br><br>`tours`: Prepared tour table.<br>&bull; `person_id` (`Int64`): Canonical person identifier associated with an individual tour.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `tour_purpose` (`String`): Primary purpose of the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `nonmandatory_tour_purpose` (`String`): nonmandatory purpose.<br>`geography_type` (`String`): home-geography system or rollup.<br>`geography_id` (`String`): home-geography identifier or rollup.<br>`average_tour_distance` (`Float64`): mean prepared `SKIMDIST` weighted by `tours.finalweight`.<br>`tour_count` (`Float64`): sum of `tours.finalweight` for tours contributing to the mean. | `processor.summarize.summaries.tour_geography.avg_non_mand_tour_distance`<br>Default build: **yes** |
| `internal_external_nonmandatory_tour_frequency_by_home_geography`<br>`internal_external_nonmandatory_tour_frequency_by_home_geography.csv` | Internal and external nonmandatory tours by traveler home geography, plus regional totals. Use it to calculate external-tour shares and locate households producing external discretionary travel. | `per`: Prepared person table.<br>&bull; `person_id` (`Int64`): Canonical person identifier.<br>&bull; `home_zone_id` (`identifier`): Prepared home-zone identifier.<br><br>`tours`: Prepared tour table.<br>&bull; `person_id` (`Int64`): Canonical person identifier associated with an individual tour.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `is_external_tour` (`Boolean-like`): Whether the tour destination is external to the modeled area.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `geography_type` (`String`): home-geography system or rollup.<br>`geography_id` (`String`): home-geography identifier or rollup.<br>`internal_nonmandatory_tour_count` (`Float64`): weighted internal nonmandatory tours.<br>`external_nonmandatory_tour_count` (`Float64`): weighted external nonmandatory tours. | `processor.summarize.summaries.tour_geography.int_vs_ext_non_mand_tour_freq`<br>Default build: **yes** |
| `external_nonmandatory_tour_locations`<br>`external_nonmandatory_tour_locations.csv` | External nonmandatory tours by destination geography, plus a regional total. Use it to analyze external destination orientation and gateway demand. | `tours`: Prepared tour table.<br>&bull; `tour_category` (`String`): Tour category such as mandatory, nonmandatory, joint, or at-work.<br>&bull; `is_external_tour` (`Boolean-like`): Whether the tour destination is external to the modeled area.<br>&bull; `destination` (`identifier`): Tour destination-zone identifier.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `geography_type` (`String`): destination-geography system or rollup.<br>`geography_id` (`String`): destination-geography identifier or rollup.<br>`external_nonmandatory_tour_count` (`Float64`): weighted external nonmandatory tours ending there. | `processor.summarize.summaries.tour_geography.ext_non_mand_tour_loc`<br>Default build: **yes** |

### Trip Purpose, Mode, and Parking

_Related dashboard pages: Trip and Stop Purpose, Trip Mode, Parking Location. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `trip_purpose_distribution`<br>`trip_purpose_distribution.csv` | Person-trip destination purpose cross-classified by parent tour purpose, with all-tour-purpose rows. Joint records use the sum of all matching participant person weights when available and valid party-size expansion only as a fallback. Use it to analyze activity chains within different kinds of tours. | `trips`: Prepared trip table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the parent tour.<br>&bull; `trip_purpose` (`String`): Destination purpose of the trip leg.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `tour_purpose` (`String`): parent tour purpose or `all_tour_purposes`.<br>`trip_purpose` (`String`): destination purpose of the trip leg.<br>`trip_count` (`Float64`): weighted person-trips in the combination. | `processor.summarize.summaries.trip.trip_purpose`<br>Default build: **yes** |
| `stop_destination_purpose_by_tour_purpose`<br>`stop_destination_purpose_by_tour_purpose.csv` | Person-stop destination purposes by parent tour purpose. Joint records use the sum of all matching participant person weights when available and valid party-size expansion only as a fallback. Use it to understand what activities are chained into tours. | `trips`: Prepared trip table.<br>&bull; `stops` (`Int32`): Intermediate-stop indicator, 1 before the last trip on a half-tour and 0 for the final leg.<br>&bull; `tour_purpose` (`String`): Primary purpose of the parent tour.<br>&bull; `trip_purpose` (`String`): Destination purpose of the trip leg.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `stop_destination_purpose` (`String`): destination purpose of trip records flagged as stops.<br>`tour_purpose` (`String`): parent tour purpose.<br>`stop_count` (`Float64`): weighted person-stops in the combination. | `processor.summarize.summaries.trip.stop_purpose_by_tour_purpose`<br>Default build: **yes** |
| `trip_mode_by_tour_purpose_and_tour_mode`<br>`trip_mode_by_tour_purpose_and_tour_mode.csv` | Person-trip mode counts by parent tour purpose and main tour mode, including all-purpose, all-tour-mode, and grand rollups. Joint records use the sum of all matching participant person weights when available; valid party-size expansion is the fallback, and sentinel, missing, or nonpositive party sizes fall back to one. Use it to examine access/egress and mode combinations within tours. | `trips`: Prepared trip table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the parent tour.<br>&bull; `tour_mode` (`String`): Main mode of the parent tour.<br>&bull; `trip_mode` (`String`): Mode used for the trip leg.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `tour_purpose` (`String`): parent purpose or `all_tour_purposes`.<br>`tour_mode` (`String`): main tour mode or `all_tour_modes`.<br>`trip_mode` (`String`): mode of the individual trip leg.<br>`trip_count` (`Float64`): weighted person-trips in the combination. | `processor.summarize.summaries.trip.trip_mode`<br>Default build: **yes** |
| `parking_locations`<br>`parking_locations.csv` | Auto-trip parking events by configured parking geography. Use it to map modeled parking demand and compare locations across runs. | `trips`: Prepared trip table.<br>&bull; `parking_zone` (`Int64`): Zone where the auto trip parks.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `geography_type` (`String`): parking-geography system.<br>`geography_id` (`String`): valid positive parking-zone identifier at that geography.<br>`trip_count` (`Float64`): weighted trips parking there. | `processor.summarize.summaries.trip.parking_locations`<br>Default build: **yes** |

### Trip Time and Distance

_Related dashboard pages: Trip and Stop Time, Trip and Stop Distance. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `trip_departure_time_by_purpose`<br>`trip_departure_time_by_purpose.csv` | Dense person-trip and person-stop departure-period profiles by parent tour purpose and for all purposes. Joint records use all matching participant person weights when available and valid party-size expansion only as a fallback. Use it to compare trip timing with stop timing. | `trips`: Prepared trip table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the parent tour.<br>&bull; `stops` (`Int32`): Intermediate-stop indicator, 1 before the last trip on a half-tour and 0 for the final leg.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `tour_purpose` (`String`): parent purpose or `all_tour_purposes`.<br>`time_bin` (`Int32`): prepared trip departure period index.<br>`departure_trip_count` (`Float64`): weighted person-trips departing in the bin.<br>`departure_stop_count` (`Float64`): weighted departing person-stops. | `processor.summarize.summaries.trip_distributions.trip_stop_tod`<br>Default build: **yes** |
| `trip_distance_by_purpose`<br>`trip_distance_by_purpose.csv` | Person-trips by lower-bound OD-distance bin and parent tour purpose, plus all-purpose totals. Joint records use the sum of all matching participant person weights when available; valid party-size expansion is the fallback, non-joint trips contribute once, and sentinel, missing, or nonpositive fallback sizes resolve to one. Use it for purpose-specific trip length distributions. | `trips`: Prepared trip table.<br>&bull; `tour_purpose` (`String`): Primary purpose of the parent tour.<br>&bull; `od_dist` (`Float64`): Origin-to-destination trip distance from the configured distance skim or supported survey distance, in source units.<br>&bull; `num_participants` (`Int32`): Number of travelers represented by a joint trip record.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `distance_bin` (`String`): exact `0`, positive sub-mile `>0-<1`, whole-mile lower bound, or `40+`.<br>`tour_purpose` (`String`): parent purpose or `all_tour_purposes`.<br>`trip_count` (`Float64`): sum of participant-adjusted trip weight in the bin. | `processor.summarize.summaries.trip_distributions.trip_distance`<br>Default build: **yes** |
| `stop_out_of_direction_distance_by_tour_purpose`<br>`stop_out_of_direction_distance_by_tour_purpose.csv` | Person-stops by lower-bound out-of-direction-distance bin, with a dense distribution for each purpose and all purposes. Joint records use all matching participant person weights when available and valid party-size expansion only as a fallback. Use it to quantify detour burden from stop-making. | `trips`: Prepared trip table.<br>&bull; `stops` (`Int32`): Intermediate-stop indicator, 1 before the last trip on a half-tour and 0 for the final leg.<br>&bull; `out_dir_dist` (`Float64`): Additional distance caused by visiting the stop rather than traveling directly to the half-tour endpoint.<br>&bull; `tour_purpose` (`String`): Primary purpose of the parent tour.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `distance_bin` (`String`): exact `0`, positive sub-mile `>0-<1`, whole-mile lower bound, or `40+`.<br>`tour_purpose` (`String`): parent purpose or `all_tour_purposes`.<br>`stop_count` (`Float64`): weighted person-stops in the bin. | `processor.summarize.summaries.trip_distributions.stop_ood_distance`<br>Default build: **yes** |

### Skimjoin Diagnostics

_Related dashboard pages: Tour Skims, Trip Skims. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `skimjoin_trip_component_stats`<br>`skimjoin_trip_component_stats.csv` | Weighted descriptive statistics for every numeric `skim_` component on trips, by trip mode and skim scenario. Use it to QA joined time, distance, and cost values and identify missing or zero-heavy components. | `trips`: Prepared trip table.<br>&bull; `trip_mode` (`String`): Mode used for the trip leg.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `skim_scenario` (`String`): chosen or hypothetical evaluation scenario.<br>`trip_mode` (`String`): trip mode or `All Modes`.<br>`component` (`String`): numeric skim column name.<br>`n_total` (`Float64`): total trip weight eligible for the component/mode group.<br>`n_valid` (`Float64`): trip weight with non-null component values, including zeros.<br>`mean` (`Float64`): weighted mean including zeros.<br>`mean_nonzero` (`Float64`): weighted mean excluding zeros.<br>`std` (`Float64`): weighted population standard deviation.<br>`min` (`Float64`): minimum observed value.<br>`max` (`Float64`): maximum observed value.<br>`median` (`Float64`): weighted 50th percentile.<br>`mode` (`Float64`): value with greatest total weight, using the smaller value on ties.<br>`zero_share` (`Float64`): valid weight at exactly zero divided by `n_valid`.<br>`missing_share` (`Float64`): missing weight divided by `n_total`. | `processor.summarize.summaries.skimjoin.trip_skim_component_stats`<br>Default build: **yes** |
| `skimjoin_trip_component_ecdf`<br>`skimjoin_trip_component_ecdf.csv` | Optional 0th-through-100th weighted percentile curves for trip skim components by mode and scenario. Use it for distribution comparison when the compact stats table is insufficient. | `trips`: Prepared trip table.<br>&bull; `trip_mode` (`String`): Mode used for the trip leg.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `skim_scenario` (`String`): chosen or hypothetical scenario.<br>`trip_mode` (`String`): trip mode or `All Modes`.<br>`component` (`String`): numeric skim column name.<br>`percentile` (`Float64`): cumulative probability from 0.00 through 1.00 in 0.01 steps.<br>`value` (`Float64`): weighted quantile at that probability.<br>`n_valid` (`Float64`): total valid trip weight behind the curve. | `processor.summarize.summaries.skimjoin.trip_skim_component_ecdf`<br>Default build: **no** |
| `skimjoin_tour_component_stats`<br>`skimjoin_tour_component_stats.csv` | Weighted descriptive statistics for numeric tour `skim_` components by tour mode and scenario. Use it to QA tour-level round-trip or composite skims. | `tours`: Prepared tour table.<br>&bull; `tour_mode` (`String`): Main mode selected for the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `skim_scenario` (`String`): chosen or hypothetical evaluation scenario.<br>`tour_mode` (`String`): tour mode or `All Modes`.<br>`component` (`String`): numeric skim column name.<br>`n_total` (`Float64`): total tour weight in scope.<br>`n_valid` (`Float64`): tour weight with a non-null value, including zeros.<br>`mean` (`Float64`): weighted mean including zeros.<br>`mean_nonzero` (`Float64`): weighted mean excluding zeros.<br>`std` (`Float64`): weighted population standard deviation.<br>`min` (`Float64`): minimum value.<br>`max` (`Float64`): maximum value.<br>`median` (`Float64`): weighted median.<br>`mode` (`Float64`): highest-weight value, smaller on ties.<br>`zero_share` (`Float64`): valid weight at zero divided by `n_valid`.<br>`missing_share` (`Float64`): missing weight divided by `n_total`. | `processor.summarize.summaries.skimjoin.tour_skim_component_stats`<br>Default build: **yes** |
| `skimjoin_tour_component_ecdf`<br>`skimjoin_tour_component_ecdf.csv` | Optional weighted percentile curves for tour skim components by mode and scenario. Use it to compare complete tour-level distributions across runs. | `tours`: Prepared tour table.<br>&bull; `tour_mode` (`String`): Main mode selected for the tour.<br>&bull; `finalweight` (`Float64`): Tour expansion weight inherited from trips, persons, households, or a 1.0 fallback according to available configured weights. | `skim_scenario` (`String`): chosen or hypothetical scenario.<br>`tour_mode` (`String`): tour mode or `All Modes`.<br>`component` (`String`): numeric skim column name.<br>`percentile` (`Float64`): probability from 0.00 through 1.00.<br>`value` (`Float64`): weighted quantile at that probability.<br>`n_valid` (`Float64`): valid tour weight behind the curve. | `processor.summarize.summaries.skimjoin.tour_skim_component_ecdf`<br>Default build: **no** |

### Processor-Built Validation Summaries

_Related dashboard pages: Overview, Traffic Validation, Transit Validation, VMT Validation. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `traffic_count_comparisons`<br>`traffic_count_comparisons.csv` | Observed and modeled traffic counts that agree at count-location, direction, and period level. Use it for count scatterplots, percent differences, RMSE, and facility calibration. The table keeps only keys in both sources. | None declared. | `count_location_id` (`String`): traffic-count station/location identifier.<br>`direction` (`String`): observed/modeled direction label.<br>`count_period` (`String`): count time-period label.<br>`observed_volume` (`Float64`): summed observed count for the key.<br>`modeled_volume` (`Float64`): summed assigned volume for the matching key. | `processor.summarize.summaries.validation.traffic_count_comparisons`<br>Default build: **yes** |
| `screenline_flow_comparisons`<br>`screenline_flow_comparisons.csv` | Observed and modeled screenline flows matched by screenline, direction, and period, with a representative facility type. Use it for corridor-level flow validation and regression analysis. | None declared. | `screenline_id` (`String`): screenline/cutline identifier.<br>`direction` (`String`): flow direction.<br>`count_period` (`String`): comparison period.<br>`facility_type` (`String`): supplied facility class, or `All` if absent.<br>`observed_volume` (`Float64`): summed observed flow.<br>`modeled_volume` (`Float64`): summed modeled flow for the matching key. | `processor.summarize.summaries.validation.screenline_flow_comparisons`<br>Default build: **yes** |
| `transit_boardings_by_operator_and_technology`<br>`transit_boardings_by_operator_and_technology.csv` | Assigned transit boardings summed by operator and transit technology. Use it to compare ridership scale across agencies and modes. | None declared. | `operator` (`String`): supplied transit operator identifier or name.<br>`technology` (`String`): supplied transit mode/technology category.<br>`boardings` (`Float64`): total assigned unlinked passenger boardings. | `processor.summarize.summaries.validation.total_transit_boardings`<br>Default build: **yes** |
| `transit_transfer_rate`<br>`transit_transfer_rate.csv` | Assigned boardings divided by linked transit trips by operator, technology, and access mode. The value is boardings per linked trip, so values above one indicate transfers; subtract one if a transfers-per-trip measure is needed. | None declared. | `operator` (`String`): transit operator.<br>`technology` (`String`): transit technology/mode.<br>`access_mode` (`String`): mode used to access transit.<br>`transfer_rate` (`Float64`): assigned boardings divided by linked trips; null for a zero linked-trip denominator. | `processor.summarize.summaries.validation.transit_transfer_rate`<br>Default build: **yes** |
| `auto_vmt_totals`<br>`auto_vmt_totals.csv` | One run-level personal-auto VMT total using the same eligible records, distance source, and legacy-compatible occupancy calculation as the detailed personal-auto VMT summary. Every eligible row is retained. Household-level joint rows use one vehicle, while repeated person-level joint rows use their fixed auto-mode occupancy. A prepared auto skim is preferred; otherwise eligible non-drive-transit trips use `od_dist`. Use it for overview controls and scenario comparison. | `trips`: Prepared trip table.<br>&bull; `trip_mode` (`String`): Mode used for the trip leg.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `auto_vmt` (`Float64`): total weighted personal-auto vehicle miles traveled. | `processor.summarize.summaries.validation.auto_vmt_totals`<br>Default build: **yes** |
| `auto_vmt_by_home_geography_income_hhsize_time_period`<br>`auto_vmt_by_home_geography_income_hhsize_time_period.csv` | Personal-auto VMT and trip counts by home geography, income, household size, time period, and auto mode. When component periods exist, Daily rows are derived only from those non-Daily rows and replace any stale source Daily row; Daily-only groups are retained. Use it for equity, temporal, modal, and spatial VMT analysis. | `trips`: Prepared trip table.<br>&bull; `trip_mode` (`String`): Mode used for the trip leg.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback. | `geography_type` (`String`): home-geography system or `all_geographies`.<br>`geography_id` (`String`): home-geography identifier or rollup.<br>`income_segment` (`String`): prepared household income segment or fallback rollup.<br>`household_size` (`String`): prepared household size or fallback rollup.<br>`time_period` (`String`): configured period or `Daily`.<br>`mode` (`String`): trip mode or `All Auto` fallback.<br>`auto_vmt` (`Float64`): sum of distance times `trips.finalweight`, divided by legacy-compatible vehicle occupancy.<br>`trip_count` (`Float64`): sum of `trips.finalweight` for eligible auto trips.<br>`distance_source` (`String`): provenance of the distance used, such as a skim or OD-distance field.<br>`time_period_source` (`String`): provenance of the time-period assignment. | `processor.summarize.summaries.validation.auto_vmt_by_home_geography_income_hhsize_time_period`<br>Default build: **yes** |
| `non_motorized_vmt_by_home_geography_income_hhsize_time_period`<br>`non_motorized_vmt_by_home_geography_income_hhsize_time_period.csv` | Walk, bicycle, and e-bike weighted miles and trip counts by home geography, income, household size, period, and mode, including derived daily rows. Use it for active-travel exposure and equity analysis. | `trips`: Prepared trip table.<br>&bull; `finalweight` (`Float64`): Trip expansion weight from the configured trip weight, inherited person or household weight, or 1.0 fallback.<br>&bull; `trip_mode` (`String`): Mode used for the trip leg. | `geography_type` (`String`): home-geography system or rollup.<br>`geography_id` (`String`): home-geography identifier or rollup.<br>`income_segment` (`String`): household income segment or fallback rollup.<br>`household_size` (`String`): household size or fallback rollup.<br>`time_period` (`String`): configured period or `Daily`.<br>`mode` (`String`): `WALK`, `BIKE`, or `EBIKE` as available.<br>`non_motorized_vmt` (`Float64`): distance times `trips.finalweight`; despite the VMT name, this is weighted traveler mileage.<br>`trip_count` (`Float64`): sum of `trips.finalweight` for eligible trips.<br>`distance_source` (`String`): prepared or skim distance source used for the mode.<br>`time_period_source` (`String`): provenance of the period assignment. | `processor.summarize.summaries.validation.non_motorized_vmt_by_home_geography_income_hhsize_time_period`<br>Default build: **yes** |
| `commercial_vmt_totals`<br>`commercial_vmt_totals.csv` | Commercial-vehicle VMT by vehicle type, split between internal and external travel. Use it for freight VMT totals and internal/external shares. | None declared. | `commercial_vehicle_type` (`String`): supplied commercial vehicle/truck class.<br>`external_vmt` (`Float64`): VMT from records classified as external.<br>`internal_vmt` (`Float64`): VMT from records classified as internal. | `processor.summarize.summaries.validation.commercial_vehicle_vmt`<br>Default build: **yes** |
| `bicycle_vmt_by_facility_type`<br>`bicycle_vmt_by_facility_type.csv` | Bicycle VMT by facility type, read directly when supplied or calculated as assigned bicycle trips times link distance. Use it to evaluate bicycle use by facility class. | None declared. | `facility_type` (`String`): supplied bicycle/network facility category.<br>`bicycle_vmt` (`Float64`): summed bicycle vehicle/traveler miles on that facility type. | `processor.summarize.summaries.validation.bicycle_vmt_by_facility`<br>Default build: **yes** |

### Externally Supplied Validation Contracts

_Related dashboard pages: Traffic Validation, VMT Validation, Regional Validation. See the [Dashboard User Guide](16-dashboard-user-guide.md#page-by-page-guide)._

| Summary Table | Information and Analytical Use | Required Tables/Fields | Output Fields | Summary Function |
|---|---|---|---|---|
| `link_validation_summary`<br>`link_validation_summary.csv` | Modeled network link volumes with link endpoints and facility class. Use it to aggregate modeled flow by facility or inspect high-volume links. | None declared. | `id` (`Int64`): link identifier.<br>`From_Node` (`Int64`): upstream node identifier.<br>`To_Node` (`Int64`): downstream node identifier.<br>`FACTYPE` (`Int64`): facility-type code.<br>`am_vol` (`Float64`): AM-period modeled link volume.<br>`md_vol` (`Float64`): midday modeled link volume.<br>`pm_vol` (`Float64`): PM-period modeled link volume.<br>`day_vol` (`Float64`): daily modeled link volume. | `processor.summarize.summaries.validation_scaffolds.link_validation_summary`<br>Default build: **no** |
| `count_location_counts_validation_summary`<br>`count_location_counts_validation_summary.csv` | Observed traffic-count volumes by count location and facility class. Use it as the observed side of location-level modeled-versus-observed comparisons. | None declared. | `id` (`Int64`): count-location identifier.<br>`FACTYPE` (`Int64`): facility-type code.<br>`am_vol` (`Float64`): observed AM volume.<br>`md_vol` (`Float64`): observed midday volume.<br>`pm_vol` (`Float64`): observed PM volume.<br>`day_vol` (`Float64`): observed daily volume. | `processor.summarize.summaries.validation_scaffolds.count_location_counts_validation_summary`<br>Default build: **no** |
| `count_location_volumes_validation_summary`<br>`count_location_volumes_validation_summary.csv` | Modeled volumes at the traffic-count locations. Use it as the modeled side of location-level count comparisons. | None declared. | `id` (`Int64`): count-location identifier matching the observed table.<br>`FACTYPE` (`Int64`): facility-type code.<br>`am_vol` (`Float64`): modeled AM volume.<br>`md_vol` (`Float64`): modeled midday volume.<br>`pm_vol` (`Float64`): modeled PM volume.<br>`day_vol` (`Float64`): modeled daily volume. | `processor.summarize.summaries.validation_scaffolds.count_location_volumes_validation_summary`<br>Default build: **no** |
| `count_location_scatter_validation_summary`<br>`count_location_scatter_validation_summary.csv` | Long-form observed/modeled point pairs already prepared for count scatterplots. Use it when the source workflow supplies paired values directly. | None declared. | `id` (`Int64`): count-location identifier.<br>`facility_type` (`String`): facility class code or label.<br>`period` (`String`): comparison period.<br>`observed_volume` (`Float64`): observed traffic volume.<br>`modeled_volume` (`Float64`): modeled traffic volume. | `processor.summarize.summaries.validation_scaffolds.count_location_scatter_validation_summary`<br>Default build: **no** |
| `count_location_fit_validation_summary`<br>`count_location_fit_validation_summary.csv` | Precomputed linear-fit diagnostics for observed-versus-modeled counts by facility type and period. Use it to draw regression lines and report calibration fit. | None declared. | `facility_type` (`String`): facility class used for the fit.<br>`period` (`String`): time period used for the fit.<br>`slope` (`Float64`): fitted slope for modeled volume as a function of observed volume.<br>`intercept` (`Float64`): fitted modeled-volume intercept.<br>`r_squared` (`Float64`): coefficient of determination.<br>`n_locations` (`Int64`): paired count locations in the fit.<br>`observed_min` (`Float64`): minimum observed volume in the fitting data.<br>`observed_max` (`Float64`): maximum observed volume.<br>`equation_label` (`String`): preformatted regression-equation text.<br>`r_squared_label` (`String`): preformatted R-squared text. | `processor.summarize.summaries.validation_scaffolds.count_location_fit_validation_summary`<br>Default build: **no** |
| `district_commuting_flows_validation_summary`<br>`district_commuting_flows_validation_summary.csv` | Supplied district-to-district commute-flow matrix for Albany, Corvallis, Lebanon, and Philomath. Use it as a local validation/control matrix. | None declared. | `""` (`String`): origin district or row label.<br>`Albany` (`Float64`): commuters to Albany.<br>`Corvallis` (`Float64`): commuters to Corvallis.<br>`Lebanon` (`Float64`): commuters to Lebanon.<br>`Philomath` (`Float64`): commuters to Philomath.<br>`Total` (`Float64`): row total across destinations. | `processor.summarize.summaries.validation_scaffolds.district_commuting_flows_validation_summary`<br>Default build: **no** |
| `county_commuting_flows_validation_summary`<br>`county_commuting_flows_validation_summary.csv` | Supplied county-to-county commute-flow matrix for Benton, Linn, and Marion counties. Use it as a regional commute-flow validation/control matrix. | None declared. | `""` (`String`): origin county or row label.<br>`Benton` (`Float64`): commuters to Benton County.<br>`Linn` (`Float64`): commuters to Linn County.<br>`Marion` (`Float64`): commuters to Marion County.<br>`Total` (`Float64`): row total across destinations. | `processor.summarize.summaries.validation_scaffolds.county_commuting_flows_validation_summary`<br>Default build: **no** |
| `commercial_vehicle_validation_summary`<br>`commercial_vehicle_validation_summary.csv` | Supplied commercial-vehicle trip totals by time of day and vehicle class. Use it to compare commercial demand composition and daily profiles. | None declared. | `tod` (`String`): time-of-day row label.<br>`car` (`Float64`): commercial-car/light-vehicle trips.<br>`mu` (`Float64`): multi-unit truck trips.<br>`su` (`Float64`): single-unit truck trips.<br>`Total` (`Float64`): total commercial trips across classes. | `processor.summarize.summaries.validation_scaffolds.commercial_vehicle_validation_summary`<br>Default build: **no** |
| `commercial_vehicle_vmt_validation_summary`<br>`commercial_vehicle_vmt_validation_summary.csv` | Supplied commercial-vehicle VMT by time of day and vehicle class. Use it to compare freight mileage composition and temporal patterns. | None declared. | `tod` (`String`): time-of-day row label.<br>`car` (`Float64`): commercial-car/light-vehicle VMT.<br>`mu` (`Float64`): multi-unit truck VMT.<br>`su` (`Float64`): single-unit truck VMT.<br>`Total` (`Float64`): total commercial VMT across classes. | `processor.summarize.summaries.validation_scaffolds.commercial_vehicle_vmt_validation_summary`<br>Default build: **no** |
| `external_trip_validation_summary`<br>`external_trip_validation_summary.csv` | Supplied external trip totals by time of day and purpose/class. Use it to analyze gateway demand by travel market. | None declared. | `tod` (`String`): time-of-day row label.<br>`hbcoll` (`Float64`): home-based college trips.<br>`hbo` (`Float64`): home-based other trips.<br>`hbr` (`Float64`): home-based recreation trips.<br>`hbs` (`Float64`): home-based shopping trips.<br>`hbsch` (`Float64`): home-based school trips.<br>`hbw` (`Float64`): home-based work trips.<br>`nhbnw` (`Float64`): non-home-based non-work trips.<br>`nhbw` (`Float64`): non-home-based work trips.<br>`truck` (`Float64`): truck trips.<br>`Total` (`Float64`): total external trips across purposes/classes. | `processor.summarize.summaries.validation_scaffolds.external_trip_validation_summary`<br>Default build: **no** |
| `external_vmt_validation_summary`<br>`external_vmt_validation_summary.csv` | Supplied external VMT by time of day and purpose/class. Use it to identify which external markets contribute mileage. | None declared. | `tod` (`String`): time-of-day row label.<br>`hbcoll` (`Float64`): home-based college VMT.<br>`hbo` (`Float64`): home-based other VMT.<br>`hbr` (`Float64`): home-based recreation VMT.<br>`hbs` (`Float64`): home-based shopping VMT.<br>`hbsch` (`Float64`): home-based school VMT.<br>`hbw` (`Float64`): home-based work VMT.<br>`nhbnw` (`Float64`): non-home-based non-work VMT.<br>`nhbw` (`Float64`): non-home-based work VMT.<br>`truck` (`Float64`): truck VMT.<br>`Total` (`Float64`): total external VMT across purposes/classes. | `processor.summarize.summaries.validation_scaffolds.external_vmt_validation_summary`<br>Default build: **no** |
| `auto_vmt_validation_summary`<br>`auto_vmt_validation_summary.csv` | Supplied auto and truck VMT by time of day and occupancy class. Use it as an independent control for modeled VMT. | None declared. | `TOD` (`String`): time-of-day row label.<br>`SOV` (`Float64`): single-occupant-vehicle VMT.<br>`HOV2` (`Float64`): two-occupant shared-ride VMT.<br>`HOV3` (`Float64`): three-or-more-occupant shared-ride VMT.<br>`Truck` (`Float64`): truck VMT.<br>`Total` (`Float64`): total VMT across listed classes. | `processor.summarize.summaries.validation_scaffolds.auto_vmt_validation_summary`<br>Default build: **no** |
| `work_from_home_validation_summary`<br>`work_from_home_validation_summary.csv` | Supplied worker and work-from-home controls by district. Use it to compare modeled WFH counts or rates with external targets. | None declared. | `District` (`String`): district name or identifier.<br>`Workers` (`Float64`): total workers in the district.<br>`WFH` (`Float64`): workers who work from home. | `processor.summarize.summaries.validation_scaffolds.work_from_home_validation_summary`<br>Default build: **no** |
<!-- GENERATED:SUMMARY-CATALOG END -->
