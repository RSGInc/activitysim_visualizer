"autoOwnership_Pre"
"zeroAutoByTaz"

For average mandatory tour distance -- let's just take the average from the tlfd results, or at least borrow heavily instead of re-calculating everything (person type thing)

for all summaries: need to be consistent about how I'm defining "worker", "student" etc. in the tlfd, worker is defined as workplace id > 0 AND is_worker, on wfh worker is defined just by workplace id > 0

Probably need to Make "district" and "county" standard aggregations

For commuting flows, maybe be more explicit about "Home Geography" and "Workplace Geography" instead of just origin and destination

